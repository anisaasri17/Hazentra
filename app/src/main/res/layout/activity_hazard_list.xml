<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/hazentra_background">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/hazardListToolbar"
        android:layout_width="0dp"
        android:layout_height="64dp"
        android:background="@color/hazentra_navy"
        android:title="Hazard Reports"
        android:titleTextColor="@color/hazentra_on_navy"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:navigationIconTint="@color/hazentra_on_navy"
        app:titleCentered="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <LinearLayout
        android:id="@+id/listHeader"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingTop="20dp"
        android:paddingEnd="20dp"
        android:paddingBottom="14dp"
        app:layout_constraintTop_toBottomOf="@id/hazardListToolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Community Hazard Reports"
            android:textColor="@color/hazentra_text"
            android:textSize="23sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/reportCountText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:text="Loading reports..."
            android:textColor="@color/hazentra_muted"
            android:textSize="13sp" />

        </LinearLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/hazardReportsRecyclerView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:clipToPadding="false"
            android:paddingStart="16dp"
            android:paddingTop="4dp"
            android:paddingEnd="16dp"
            android:paddingBottom="24dp"
            app:layout_constraintTop_toBottomOf="@id/listHeader"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <LinearLayout
            android:id="@+id/emptyStateContainer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="28dp"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/listHeader"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/emptyStateTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No active hazard reports"
            android:textColor="@color/hazentra_text"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/emptyStateMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:text="There are currently no active reports available."
            android:textColor="@color/hazentra_muted"
            android:textSize="13sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/retryHazardListButton"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="18dp"
            android:text="Retry"
            android:textAllCaps="false"
            android:textColor="@color/hazentra_orange"
            android:textStyle="bold"
            android:visibility="gone"
            app:backgroundTint="@color/hazentra_surface"
            app:cornerRadius="14dp"
            app:strokeColor="@color/hazentra_orange"
            app:strokeWidth="1dp" />

    </LinearLayout>

    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/hazardListLoadingIndicator"
        android:layout_width="54dp"
        android:layout_height="54dp"
        app:indicatorColor="@color/hazentra_orange"
        app:indicatorSize="54dp"
        app:trackThickness="5dp"
        app:layout_constraintTop_toBottomOf="@id/listHeader"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>